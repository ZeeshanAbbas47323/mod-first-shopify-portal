import type { Metadata } from "next";

export const metadata: Metadata = { title: "Design uploads" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
